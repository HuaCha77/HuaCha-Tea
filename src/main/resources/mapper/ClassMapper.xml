<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.evaluation.dao.ClassMapper">
    <select id="findAll" resultType="com.edu.evaluation.entity.ClassInfo">
        SELECT * FROM sys_class WHERE del_flag = 0 ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.edu.evaluation.entity.ClassInfo">
        INSERT INTO sys_class(class_name, major, grade, create_time, del_flag)
        VALUES(#{className}, #{major}, #{grade}, NOW(), 0)
    </insert>

    <update id="deleteById">
        UPDATE sys_class SET del_flag = 1 WHERE id = #{id}
    </update>
</mapper>